<preference-headers xmlns:android="http://schemas.android.com/apk/res/android"
                    xmlns:unified="http://schemas.android.com/apk/res-auto" >

    <!--
	/*
	** Header definitions are identical to android:* except for preferenceRes which should be a reference to a preference xml file
	** Unlike the native headers these are also used for building the single pane version.
	*/
    -->
    <header
            unified:fragment="com.developer.taxometer.activities.ConfigurationActivity$GeneralPreferenceFragment"
            unified:title="@string/preference_header_general"
            unified:preferenceRes="@xml/preference_general" />
    <header
            unified:fragment="com.developer.taxometer.activities.ConfigurationActivity$GPSPreferenceFragment"
            unified:title="@string/preference_header_gps_config"
            unified:preferenceRes="@xml/preference_gps_config" />

</preference-headers>